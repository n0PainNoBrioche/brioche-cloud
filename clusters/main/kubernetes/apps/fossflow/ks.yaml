apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fossflow
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/fossflow/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

