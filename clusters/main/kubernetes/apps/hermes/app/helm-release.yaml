apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hermes-agent
  namespace: hermes-agent
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 18.4.3
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 5m
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    image:
      repository: nousresearch/hermes-agent
      tag: latest
      pullPolicy: Always

    service:
      main:
        ports:
          main:
            enabled: true
            protocol: http
            port: 8642
            targetPort: 8642

    securityContext:
      container:
        runAsNonRoot: false
        runAsUser: 0
        runAsGroup: 0
        readOnlyRootFilesystem: false
      pod:
        fsGroup: 568

    workload:
      main:
        podSpec:
          containers:
            main:
              args:
                - "gateway"
                - "run"
              env:
                HERMES_UID: "568"
                HERMES_GID: "568"
                HERMES_HOME: "/opt/data"
                OLLAMA_HOST: "http://ollama-api.ollama.svc.cluster.local:11434"
                API_SERVER_HOST: "0.0.0.0"
                API_SERVER_KEY: "${AI_API_KEY}"
              resources:
                requests:
                  memory: 512Mi
                  cpu: "100m"
                limits:
                  memory: 2Gi
                  cpu: "1"

    persistence:
      hermes-data:
        enabled: true
        type: pvc
        accessMode: ReadWriteOnce
        size: 5Gi
        mountPath: /opt/data