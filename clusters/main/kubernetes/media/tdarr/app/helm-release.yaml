apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tdarr
  namespace: media
spec:
  interval: 10m
  chart:
    spec:
      chart: tdarr
      version: 13.22.0
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 5m
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  timeout: 20m
  values:
    portal:
      open:
        enabled: false
    global:
      stopAll: false
    exporterImage:
      repository: docker.io/homeylab/tdarr-exporter
      pullPolicy: IfNotPresent
      tag: 3.0.0@sha256:07bfc1b72ecec73a343be2007c22373b6e15acbdbecdd29545ab5a03ca21ca69
    credentials:
      minio:
        type: s3
        url: "${S3_URL_MINIO}"
        bucket: "${S3_BUCKET}-jellyfin"
        accessKey: "${S3_ACCESS_KEY}"
        secretKey: "${S3_SECRET_KEY}"
        encrKey: "${S3_ENCRYPTION_KEY}"
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        integrations:
          certManager:
            enabled: true
            certificateIssuer: domain-0-le-prod
          nginx:
            enabled: true
            auth:
              type: "authentik"
              internalHost: "authentik-http.authentik.svc.cluster.local:10230"
              externalHost: "portal.${DOMAIN_0}"
              responseHeaders: []
        hosts:
          - host: tdarr.${DOMAIN_0}
    persistence:
      configs:
        volsync:
        - name: config
          type: restic
          credentials: minio
          dest:
            enabled: true
          src:
            enabled: true
            trigger:
              schedule: 50 0 * * *
      media:
        enabled: true
        existingClaim: media-nfs-pvc
      transcode-cache:
        enabled: false
    service:
      metrics:
        enabled: "{{ .Values.metrics.main.enabled }}"
        ports:
          metrics:
            enabled: true
            port: 9090
    workload:
      main:
        podSpec:
          containers:
            exportarr:
              enabled: "{{ .Values.metrics.main.enabled }}"
              imageSelector: exporterImage
              probes:
                liveness:
                  enabled: false
                readiness:
                  enabled: false
                startup:
                  enabled: false
              env:
                TDARR_URL: "http://tdarr.media.svc.cluster.local:8265"
    metrics:
      main:
        enabled: true
        endpoints:
          - port: metrics
            path: /metrics
        targetSelector: metrics      
    resources:
      limits:
        cpu: 4000m
        memory: 8Gi
        nvidia.com/gpu: 1