apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: external-service-dokploy
    namespace: external-service
spec:
    interval: 5m
    chart:
        spec:
            chart: external-service
            version: 17.6.0
            sourceRef:
                kind: HelmRepository
                name: truecharts
                namespace: flux-system
            interval: 5m
    install:
        createNamespace: true
        crds: CreateReplace
        remediation:
            retries: 3
    upgrade:
        crds: CreateReplace
        remediation:
            retries: 3
    values:
      portal:
        open:
          enabled: false
      service:
        main:
          enabled: true
          externalIP: ${DOKPLOY_IP}
          ports:
            main:
              enabled: true
              port: 3000
              protocol: http
          type: ExternalIP
      ingress:
        main:
          enabled: true
          primary: true
          ingressClassName: external
          hosts:
          - host: dokploy.${DOMAIN_0}
            paths:
            - path: /
              pathType: Prefix
          integrations:
            certManager:
              certificateIssuer: domain-0-le-prod
              enabled: true
            nginx:
              enabled: true
              auth:
                type: "authentik"
                internalHost: "authentik-http.authentik.svc.cluster.local:10230"
                externalHost: "portal.${DOMAIN_0}"
                responseHeaders: []
        api:
          enabled: true
          ingressClassName: external
          hosts:
          - host: dokploy.${DOMAIN_0}
            paths:
            - path: /api
              pathType: Prefix
          integrations:
            certManager:
              certificateIssuer: domain-0-le-prod
              enabled: true
          annotations:
            nginx.ingress.kubernetes.io/enable-global-auth: "false"