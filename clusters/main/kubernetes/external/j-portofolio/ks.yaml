apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: j-portofolio
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/external/j-portofolio/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

